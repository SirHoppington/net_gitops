# Copy to gh_runner/.env and fill in. ACCESS_TOKEN = PAT with repo + admin:repo_hook
# (manage_runners:org not needed for a repo-scoped runner).
OWNER=SirHoppington
REPO=net_gitops
ACCESS_TOKEN=ghp_xxx
