## Repository to test GitOps with Clabernetes

This repo contains:

1. A GitHub self-hosted runner Docker image, to allow GH Actions to trigger argoCD running on a locally

2. A simple PyATS Dockerfile and script triggered via GH Actions to test the GitOps workflow.